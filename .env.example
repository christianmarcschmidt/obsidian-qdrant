# Obsidian Vault Configuration
VAULT_DIR=/path/to/obsidian

# Qdrant Configuration
# Options: local, cloud
QDRANT_MODE=local

# Local Qdrant settings (used when QDRANT_MODE=local)
QDRANT_HOST=localhost
QDRANT_PORT=6333

# Qdrant Cloud settings (OPTIONAL - only required when QDRANT_MODE=cloud)
# QDRANT_URL=https://your-cluster-url.qdrant.tech
# QDRANT_API_KEY=your-qdrant-cloud-api-key

# Collection Configuration
# You can use any collection name you prefer
COLLECTION=obsidian_rag

# Embedding Configuration
# Options: huggingface, openai
EMBEDDING_PROVIDER=huggingface

# HuggingFace Model (used when EMBEDDING_PROVIDER=huggingface)
MODEL_ID=sentence-transformers/all-MiniLM-L6-v2

# OpenAI Configuration (OPTIONAL - only required when EMBEDDING_PROVIDER=openai)
# OPENAI_API_KEY=sk-your-openai-api-key-here

# NOTE: Different embedding providers produce different vector dimensions:
# - HuggingFace models typically produce 384-dimensional vectors
# - OpenAI text-embedding-3-small produces 1536-dimensional vectors
# 
# If you switch between providers, you may need to:
# 1. Use a different collection name, OR
# 2. Delete and recreate your collection with the new dimensions
